// Generated by com.stripe.generator.entity.SdkBuilder

package com.stripe.param.issuing;

import com.google.gson.annotations.SerializedName;
import com.stripe.net.ApiRequestParams;
import java.util.ArrayList;
import java.util.List;
import lombok.Getter;

@Getter
public class VerificationCreateParams extends ApiRequestParams {
  /** The `Card` for which a verification is requested. */
  @SerializedName("card")
  String card;

  /** Specifies which fields in the response should be expanded. */
  @SerializedName("expand")
  List<String> expand;

  /** The scope of the verification (one of card_pin_retrieve, card_pin_update). */
  @SerializedName("scope")
  Scope scope;

  /** The method used to send the cardholder the verification (one of email, sms). */
  @SerializedName("verification_method")
  VerificationMethod verificationMethod;

  private VerificationCreateParams(
      String card, List<String> expand, Scope scope, VerificationMethod verificationMethod) {
    this.card = card;
    this.expand = expand;
    this.scope = scope;
    this.verificationMethod = verificationMethod;
  }

  public static Builder builder() {
    return new com.stripe.param.issuing.VerificationCreateParams.Builder();
  }

  public static class Builder {
    private String card;

    private List<String> expand;

    private Scope scope;

    private VerificationMethod verificationMethod;

    /** Finalize and obtain parameter instance from this builder. */
    public VerificationCreateParams build() {
      return new VerificationCreateParams(
          this.card, this.expand, this.scope, this.verificationMethod);
    }

    /**
     * Add an element to `expand` list. A list is initialized for the first call, and subsequent
     * calls adds additional elements to the original list. See {@link
     * VerificationCreateParams#expand} for the field documentation.
     */
    public Builder addExpand(String element) {
      if (this.expand == null) {
        this.expand = new ArrayList<>();
      }
      this.expand.add(element);
      return this;
    }

    /** The `Card` for which a verification is requested. */
    public Builder setCard(String card) {
      this.card = card;
      return this;
    }

    /** The scope of the verification (one of card_pin_retrieve, card_pin_update). */
    public Builder setScope(Scope scope) {
      this.scope = scope;
      return this;
    }

    /** The method used to send the cardholder the verification (one of email, sms). */
    public Builder setVerificationMethod(VerificationMethod verificationMethod) {
      this.verificationMethod = verificationMethod;
      return this;
    }
  }

  public enum Scope implements ApiRequestParams.Enum {
    @SerializedName("card_pin_retrieve")
    CARD_PIN_RETRIEVE("card_pin_retrieve"),

    @SerializedName("card_pin_update")
    CARD_PIN_UPDATE("card_pin_update");

    @Getter private final String value;

    Scope(String value) {
      this.value = value;
    }
  }

  public enum VerificationMethod implements ApiRequestParams.Enum {
    @SerializedName("email")
    EMAIL("email"),

    @SerializedName("sms")
    SMS("sms");

    @Getter private final String value;

    VerificationMethod(String value) {
      this.value = value;
    }
  }
}
