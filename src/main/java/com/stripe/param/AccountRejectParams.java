// Generated by com.stripe.generator.entity.SdkBuilder

package com.stripe.param;

import com.google.gson.annotations.SerializedName;
import com.stripe.net.ApiRequestParams;
import java.util.ArrayList;
import java.util.List;

public class AccountRejectParams extends ApiRequestParams {
  /** Specifies which fields in the response should be expanded. */
  @SerializedName("expand")
  List<String> expand;

  /** The reason for rejecting the account. Can be `fraud`, `terms_of_service`, or `other`. */
  @SerializedName("reason")
  String reason;

  private AccountRejectParams(List<String> expand, String reason) {
    this.expand = expand;
    this.reason = reason;
  }

  public static Builder builder() {
    return new com.stripe.param.AccountRejectParams.Builder();
  }

  public static class Builder {
    private List<String> expand;

    private String reason;

    /** Finalize and obtain parameter instance from this builder. */
    public AccountRejectParams build() {
      return new AccountRejectParams(this.expand, this.reason);
    }

    /**
     * Add an element to `expand` list. A list is initialized for the first `add/addAll` call, and
     * subsequent calls adds additional elements to the original list. See {@link
     * AccountRejectParams#expand} for the field documentation.
     */
    public Builder addExpand(String element) {
      if (this.expand == null) {
        this.expand = new ArrayList<>();
      }
      this.expand.add(element);
      return this;
    }

    /**
     * Add all elements to `expand` list. A list is initialized for the first `add/addAll` call, and
     * subsequent calls adds additional elements to the original list. See {@link
     * AccountRejectParams#expand} for the field documentation.
     */
    public Builder addAllExpand(List<String> elements) {
      if (this.expand == null) {
        this.expand = new ArrayList<>();
      }
      this.expand.addAll(elements);
      return this;
    }

    /** The reason for rejecting the account. Can be `fraud`, `terms_of_service`, or `other`. */
    public Builder setReason(String reason) {
      this.reason = reason;
      return this;
    }
  }
}
