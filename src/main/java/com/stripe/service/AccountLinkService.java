// Generated by com.stripe.generator.entity.SdkBuilder

package com.stripe.service;

import com.stripe.exception.StripeException;
import com.stripe.model.AccountLink;
import com.stripe.net.ApiResource;
import com.stripe.net.ApiService;
import com.stripe.net.RequestOptions;
import com.stripe.param.AccountLinkCreateParams;

public class AccountLinkService extends ApiService {
  /**
   * Creates an AccountLink object that returns a Stripe URL that the user can redirect their user
   * to in order to take them through the Hosted Verification flow.
   */
  public AccountLink create(AccountLinkCreateParams params) throws StripeException {
    return create(params, (RequestOptions) null);
  }

  /**
   * Creates an AccountLink object that returns a Stripe URL that the user can redirect their user
   * to in order to take them through the Hosted Verification flow.
   */
  public AccountLink create(AccountLinkCreateParams params, RequestOptions options)
      throws StripeException {
    String url = resourceUrl("/v1/account_links");
    return request(ApiResource.RequestMethod.POST, url, params, AccountLink.class, options);
  }
}
