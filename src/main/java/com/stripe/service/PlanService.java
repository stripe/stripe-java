// Generated by com.stripe.generator.entity.SdkBuilder

package com.stripe.service;

import com.stripe.exception.StripeException;
import com.stripe.model.Plan;
import com.stripe.model.PlanCollection;
import com.stripe.net.ApiResource;
import com.stripe.net.ApiService;
import com.stripe.net.RequestOptions;
import com.stripe.param.PlanCreateParams;
import com.stripe.param.PlanDeleteParams;
import com.stripe.param.PlanListParams;
import com.stripe.param.PlanRetrieveParams;
import com.stripe.param.PlanUpdateParams;

public class PlanService extends ApiService {
  /**
   * You can create plans using the API, or in the Stripe <a
   * href="https://dashboard.stripe.com/subscriptions/products">Dashboard</a>.
   */
  public Plan create(PlanCreateParams params) throws StripeException {
    return create(params, (RequestOptions) null);
  }

  /**
   * You can create plans using the API, or in the Stripe <a
   * href="https://dashboard.stripe.com/subscriptions/products">Dashboard</a>.
   */
  public Plan create(PlanCreateParams params, RequestOptions options) throws StripeException {
    String url = resourceUrl("/v1/plans");
    return request(ApiResource.RequestMethod.POST, url, params, Plan.class, options);
  }

  /** Deleting plans means new subscribers can’t be added. Existing subscribers aren’t affected. */
  public Plan delete(String plan, PlanDeleteParams params) throws StripeException {
    return delete(plan, params, (RequestOptions) null);
  }

  /** Deleting plans means new subscribers can’t be added. Existing subscribers aren’t affected. */
  public Plan delete(String plan, PlanDeleteParams params, RequestOptions options)
      throws StripeException {
    String url = resourceUrl("/v1/plans/%s", plan);
    return request(ApiResource.RequestMethod.DELETE, url, params, Plan.class, options);
  }

  /** Returns a list of your plans. */
  public PlanCollection list(PlanListParams params) throws StripeException {
    return list(params, (RequestOptions) null);
  }

  /** Returns a list of your plans. */
  public PlanCollection list(PlanListParams params, RequestOptions options) throws StripeException {
    String url = resourceUrl("/v1/plans");
    return requestCollection(url, params, PlanCollection.class, options);
  }

  /** Retrieves the plan with the given ID. */
  public Plan retrieve(String plan, PlanRetrieveParams params) throws StripeException {
    return retrieve(plan, params, (RequestOptions) null);
  }

  /** Retrieves the plan with the given ID. */
  public Plan retrieve(String plan, PlanRetrieveParams params, RequestOptions options)
      throws StripeException {
    String url = resourceUrl("/v1/plans/%s", plan);
    return request(ApiResource.RequestMethod.GET, url, params, Plan.class, options);
  }

  /**
   * Updates the specified plan by setting the values of the parameters passed. Any parameters not
   * provided are left unchanged. By design, you cannot change a plan’s ID, amount, currency, or
   * billing cycle.
   */
  public Plan update(String plan, PlanUpdateParams params) throws StripeException {
    return update(plan, params, (RequestOptions) null);
  }

  /**
   * Updates the specified plan by setting the values of the parameters passed. Any parameters not
   * provided are left unchanged. By design, you cannot change a plan’s ID, amount, currency, or
   * billing cycle.
   */
  public Plan update(String plan, PlanUpdateParams params, RequestOptions options)
      throws StripeException {
    String url = resourceUrl("/v1/plans/%s", plan);
    return request(ApiResource.RequestMethod.POST, url, params, Plan.class, options);
  }
}
