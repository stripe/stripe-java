// Generated by com.stripe.generator.entity.SdkBuilder

package com.stripe.service;

import com.stripe.exception.StripeException;
import com.stripe.model.CountrySpec;
import com.stripe.model.CountrySpecCollection;
import com.stripe.net.ApiResource;
import com.stripe.net.ApiService;
import com.stripe.net.RequestOptions;
import com.stripe.param.CountrySpecListParams;
import com.stripe.param.CountrySpecRetrieveParams;

public class CountrySpecService extends ApiService {
  /** Lists all Country Spec objects available in the API. */
  public CountrySpecCollection list(CountrySpecListParams params) throws StripeException {
    return list(params, (RequestOptions) null);
  }

  /** Lists all Country Spec objects available in the API. */
  public CountrySpecCollection list(CountrySpecListParams params, RequestOptions options)
      throws StripeException {
    String url = resourceUrl("/v1/country_specs");
    return requestCollection(url, params, CountrySpecCollection.class, options);
  }

  /** Returns a Country Spec for a given Country code. */
  public CountrySpec retrieve(String country, CountrySpecRetrieveParams params)
      throws StripeException {
    return retrieve(country, params, (RequestOptions) null);
  }

  /** Returns a Country Spec for a given Country code. */
  public CountrySpec retrieve(
      String country, CountrySpecRetrieveParams params, RequestOptions options)
      throws StripeException {
    String url = resourceUrl("/v1/country_specs/%s", country);
    return request(ApiResource.RequestMethod.GET, url, params, CountrySpec.class, options);
  }
}
